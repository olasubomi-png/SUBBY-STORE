# Seller wallet (Phase 9.3)
Ledger-based kobo accounting. Earnings: `order_earning:{orderId}` once on paid order. Withdrawals: hold → Paystack transfer → success/fail/reverse webhooks. Debt on refund after payout. `MIN_WITHDRAWAL_KOBO` default 100000. Enable Paystack Transfers.
